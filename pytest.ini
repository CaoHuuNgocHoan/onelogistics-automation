# pytest.ini
[pytest]
# Hiển thị output chi tiết
addopts = -v --tb=short

# Playwright settings
# Chạy trên Chromium, có mở browser để xem (headed mode)
# Khi đã ổn định, đổi thành --browser chromium (không có --headed) để chạy nhanh